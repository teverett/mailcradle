package com.khubla.kmailsorter.command;

import javax.mail.*;

import com.khubla.kmailsorter.domain.*;

public class RequireSieveCommand implements SieveCommand {
	@Override
	public void execute(Message message, Command command) {
		// we good
	}
}
